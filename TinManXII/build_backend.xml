<?xml version="1.0" encoding="UTF-8"?>

<project name="FRC Deployment Backend" default="deploy">

  <!--
  The following properties can be defined to override system level
  settings. These should not be touched unless you know what you're
  doing. The primary use is to override the wpilib version when
  working with older robots that can't compile with the latest
  libraries.
  -->

    <!-- By default the system version of WPI is used -->
    <!-- <property name="version" value=""/> -->

    <!-- By default the system team number is used -->
    <!-- <property name="team-number" value=""/> -->

    <!-- By default the target is set to 10.TE.AM.2 -->
    <!-- <property name="target" value=""/> -->

    <!-- Any other property in build.properties can also be overridden. -->
  
    <property file="${user.home}/wpilib/wpilib.properties"/>
    <property file="build.properties"/>
    <property file="${user.home}/wpilib/java/${version}/ant/build.properties"/>
	<property name="ExtraArgs" value="-parameters"/>
	
	<!-- This might not be quite correct -->
  	<property name="cfg.xml" value="${TinManRepo.dir}/TinManXII.cfg.xml" />
	<property name="left_recording.csv" value="${TinManRepo.dir}/LeftRecording.csv" />
	<property name="right_recording.csv" value="${TinManRepo.dir}/RightRecording.csv" />

	<property name="TinManSrc.dir" value="${TinManRepo.dir}/TinManXII/src" />
	
    <import file="newWPIBuild.xml" />
	

</project> 
